<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="acct">
	<select id="selectEmployee" resultType="map">
		select 
		    (select sum(salrealpay+saltax) from salary) as salaries,
		    (select sum(saltax) from salary) as saltax,
		    (select sum(sevpay) from severance) as severance,
		    (select sum(BTPROOM+BTPTRANSPORTATION+BTPENTERTAIN) from businessTripPay) as biztrip
		from dual
	</select>
</mapper>
