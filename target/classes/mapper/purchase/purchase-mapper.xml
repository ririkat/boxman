<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="purchase">

	<select id="selectPurList" resultType="map">
		SELECT DISTINCT a.*, b.CONNAME, c.*, d.EMPNAME
		FROM PURCHASETAB a, CONNECTION b, PURITEMLIST c, EMPLOYEE d
		WHERE a.CONCODE=b.CONCODE(+) and a.PITEMS=c.PITEMS(+) and a.EMPNO=d.EMPNO(+)
		ORDER BY PURCODE
	</select>
	
	<select id="selectPurCount" resultType="_int">
		SELECT COUNT(*) FROM PURCHASETAB
	</select>
	
	<select id="selectConnList" resultType="map">
		SELECT * FROM CONNECTION WHERE CONCATEG='매입'
	</select>

</mapper>
